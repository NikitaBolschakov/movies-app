/* import { Input } from 'antd';

const { Search } = Input;

const SearchBar = ({ onSearch }) => {
  return <Search placeholder="Введите название фильма" onSearch={onSearch} enterButton />;
};

export default SearchBar; */
